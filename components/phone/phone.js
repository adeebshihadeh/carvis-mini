var phone = {
  hasFocus: function() {
    return true;
  },
  handleInput: function(input) {
    if (this.hasFocus()) {
      if (isNaN(input)) {
        // add number 
      }
    }
  },
  init: function() {

  }
};